# Project Notes

This is a standalone ECHO Events Hub deployment.

- Use pnpm for dependency management.
- Keep Supabase runtime values in the hosting environment, not in source files.
- Do not activate external sync workflows without explicit approval.
